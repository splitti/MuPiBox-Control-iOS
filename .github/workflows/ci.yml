name: Core CI

on:
  push:
  pull_request:

jobs:
  swift-core:
    runs-on: ubuntu-latest
    container: swift:latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Test MuPiBoxCore
        run: swift test
